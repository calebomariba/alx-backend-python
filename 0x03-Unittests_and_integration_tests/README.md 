# alx-backend-python

This repository contains Python code for the ALX Backend Python project, focusing on unit tests and integration tests.

## Directory: 0x03-Unittests_and_integration_tests

This directory contains implementations and tests for utility functions used in a GitHub organization client.

### Files

- **utils.py**: Contains utility functions for accessing nested maps, fetching JSON from URLs, and memoizing methods.
- **test_utils.py**: Contains unit tests for the `access_nested_map` function using the `unittest` framework and `parameterized` for parameterized testing.

### Requirements

- All files are interpreted/compiled on **Ubuntu 18.04 LTS** using **Python 3.7**.
- Files start with `#!/usr/bin/env python3`.
- Code adheres to **pycodestyle (version 2.5)**.
- All files are executable.
- Modules, classes, and functions have descriptive docstrings.
- Functions and coroutines are type-annotated.
- Tests use the `unittest` framework and `parameterized` for parameterized testing.

### Setup

1. Ensure Python 3.7 is installed on Ubuntu 18.04 LTS.
2. Install required packages:
   ```bash
   pip install requests parameterized pycodestyle